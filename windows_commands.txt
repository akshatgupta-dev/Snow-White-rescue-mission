cd "C:\Users\gupta\Desktop"

Remove-Item -Recurse -Force "C:\Users\gupta\Desktop\the-adventures-of-cinderella" -ErrorAction SilentlyContinue

robocopy "\\wsl.localhost\Ubuntu\home\akshatgg\applied_programming\second_course\the-adventures-of-cinderella" "C:\Users\gupta\Desktop\the-adventures-of-cinderella" /E /XD .venv __pycache__ build dist

cd "C:\Users\gupta\Desktop\the-adventures-of-cinderella"

Remove-Item -Recurse -Force .venv, build, dist -ErrorAction SilentlyContinue
Remove-Item -Force "Cinderella Rescue Mission.spec" -ErrorAction SilentlyContinue

py -3.12 -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1

python -m pip install --upgrade pip
pip install pyinstaller pillow opencv-python pyttsx3 edge-tts

pip install -r requirements.txt

python main_ui.py

Remove-Item -Recurse -Force build, dist -ErrorAction SilentlyContinue
Remove-Item -Force "Cinderella Rescue Mission.spec" -ErrorAction SilentlyContinue

pyinstaller `
  --noconfirm `
  --clean `
  --windowed `
  --name "Cinderella Rescue Mission" `
  --add-data "src\assets;src\assets" `
  --hidden-import "PIL.Image" `
  --hidden-import "PIL.ImageTk" `
  --hidden-import "cv2" `
  main_ui.py

& ".\dist\Cinderella Rescue Mission\Cinderella Rescue Mission.exe"